{
  "name": "hotel-db-project-1",
  "version": "1.0.0",
  "description": "Advanced Hotel Management System using MongoDB",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "migrate": "node scripts/migrations/001_initial_collections.js && node scripts/migrations/002_add_indexes.js",
    "seed": "node scripts/seeds/rooms.seed.js && node scripts/seeds/personnel.seed.js && node scripts/seeds/foods.seed.js && node scripts/seeds/events.seed.js && node scripts/seeds/parkingSpaces.seed.js && node scripts/seeds/visitors.seed.js"
  },
  "keywords": ["mongodb", "hotel", "management", "database", "nodejs"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.3.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  }
}